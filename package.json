{
  "name": "my-html-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && npm run preview",
    "start:prod": "vite build --mode production && vite preview --port 4173 --host",
    "deploy": "npm install && npm run build && npm run preview",
    "serve": "npm run build && npx serve -s dist",
    "clean": "rm -rf dist node_modules",
    "fresh-start": "npm run clean && npm install && npm run start"
  },
  "devDependencies": {
    "nexe": "^5.0.0-beta.4",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "apexcharts": "^4.7.0",
    "serve": "^14.2.1"
  }
}